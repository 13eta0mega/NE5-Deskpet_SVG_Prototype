# SVG Character Animator 상세 분석과 구현 대응

분석 대상:

- [molauu/svg-character-animator](https://github.com/molauu/svg-character-animator)
- [Animator Studio English](https://molauu.github.io/svg-character-animator/animator-studio-en.html)

## 1. 프로젝트가 해결하는 문제

이 프로젝트의 핵심은 여러 SVG 이미지를 단순히 교체하는 것이 아니라, 하나의 캐릭터가 상태 사이를 살아 있는 것처럼 이동하게 만드는 것입니다. 각 상태는 같은 캔버스와 같은 파츠 ID를 사용합니다. 런타임은 현재 렌더링된 파츠의 실제 값을 읽고 목적 상태의 값까지 보간합니다.

이 구조가 중요한 이유는 N개의 상태에 대해 N x N개의 전환을 직접 만들 필요가 없기 때문입니다. 상태가 16개여도 전환 함수는 하나입니다. 사용자가 전환 도중 다른 상태를 선택하면 기존 전환을 중단하고, 그 순간 화면에 보이는 값을 새 출발점으로 사용합니다. 그래서 중립 상태로 되돌아갔다가 다시 움직이는 점프가 생기지 않습니다.

## 2. 저장소 구조

원본 저장소는 다음 층으로 나뉩니다.

- `SKILL.md`: 에이전트가 SVG 상태 머신을 만드는 전체 작업 순서
- `templates/MorphAnimator.jsx`: React 기반 프로덕션 모프 엔진 템플릿
- `templates/preview/engine.jsx`: Animator Studio가 사용하는 편집 및 미리보기 엔진
- `templates/preview/app.jsx`: 상태 선택, 프리셋, 디버그 UI
- `references/`: 경로 정규화, 전략 감지, idle, 상태 머신, iOS 변환 설명
- `examples/*.states.js`: 공통 파츠 ID와 상태별 경로 데이터를 가진 예제
- `assets/preset-library.json`: easing과 idle 프리셋
- `scripts/`: 단독 HTML 빌드, Figma 변환, SwiftUI 내보내기

생성된 `animator-studio-en.html`은 편집 원본이 아니라 빌드 결과입니다. 실제 변경은 `templates/preview`에서 한 뒤 빌드 스크립트로 하나의 HTML에 합치는 방식입니다.

## 3. 상태 데이터 모델

각 상태는 크게 세 정보를 가집니다.

1. 같은 `viewBox`
2. `id`를 키로 하는 파츠 데이터
3. 상태에 맞는 idle 설정

예를 들어 `eyeL`, `eyeR`, `mouth`, `body`가 모든 상태에 있으면 이 파츠들은 공유 파츠가 됩니다. 특정 상태에만 있는 장식은 orphan 파츠로 분류됩니다. 공유 파츠는 보간되고, orphan 파츠는 나타나거나 사라질 때 opacity로 처리됩니다.

좋은 입력 데이터의 조건은 다음과 같습니다.

- 모든 상태가 같은 캔버스와 `viewBox`를 사용해야 합니다.
- 해부학적으로 같은 파츠는 모든 상태에서 같은 ID를 가져야 합니다.
- 눈, 입, 귀, 꼬리처럼 독립적으로 움직일 부분은 합친 경로가 아니라 별도 파츠여야 합니다.
- 실제로 상태에 따라 생기고 사라지는 장식만 고유 ID를 사용해야 합니다.
- 바닥에 서는 캐릭터는 발 위치가 최대한 고정되어야 합니다.

## 4. 경로 처리 전략

원본 엔진은 모든 파츠를 같은 모프 라이브러리에 넣지 않습니다. 먼저 각 경로를 정규화하고, 명령 구조의 fingerprint를 비교합니다.

### 4.1 정규화

SVG는 같은 모양도 여러 명령으로 표현할 수 있습니다. 상대 좌표와 절대 좌표, `H`와 `V`, `L`, 원과 타원의 기본 요소, transform 속성 등이 대표적입니다. 엔진은 이 차이를 정규화해서 실제로 같은 구조인 경로를 같은 것으로 인식합니다.

정규화를 충분히 하지 않으면 같은 타원형 눈도 불필요하게 polygon 모프를 거치며 찌그러질 수 있습니다.

### 4.2 TRANSFORM 전략

정규화한 명령 구조가 모든 상태에서 같으면 각 베지어 제어점과 좌표를 직접 선형 보간합니다. 이 방식은 원래 곡선과 stroke 특성을 유지합니다. 작은 눈, 입, 버튼 같은 파츠에 특히 안정적입니다.

SVG `transform` 속성도 경로 좌표와 함께 보간해야 합니다. 경로만 바뀌고 회전 transform이 처음이나 끝에서 갑자기 적용되면 한 프레임의 점프가 보입니다.

### 4.3 MORPH 전략

경로 명령 구조가 실제로 다를 때만 flubber 계열의 polygon 보간을 사용합니다. 작은 파츠에는 더 촘촘한 샘플링을 적용하고 큰 파츠는 조금 느슨한 값을 사용합니다.

polygon 모프는 열린 stroke, 여러 subpath, 매우 작은 곡선에서 품질이 쉽게 무너집니다. 따라서 이 전략은 기본값이 아니라 구조가 다른 파츠의 fallback입니다.

### 4.4 FADE 전략

한 상태에만 존재하는 눈물, 별, 꽃 같은 장식은 없는 형태에서 억지로 모프하지 않습니다. 해당 상태에 들어갈 때 fade in하고 나올 때 fade out합니다.

### 4.5 다중 subpath

하나의 `d`에 여러 `M` 명령이 있는 경로는 렌더 단계에서 개별 path로 나누는 편이 안전합니다. 모프 라이브러리가 첫 subpath만 처리하거나 열린 선의 cap을 망가뜨리는 문제를 줄일 수 있습니다.

## 5. 상태 전환 엔진

전환 함수의 처리 순서는 다음과 같습니다.

1. 진행 중인 전환이 있으면 중단합니다.
2. DOM에서 각 파츠의 현재 렌더 값을 읽습니다.
3. 목적 상태의 파츠 데이터를 가져옵니다.
4. 파츠별 전략에 따라 현재 값에서 목적 값까지 보간합니다.
5. idle 진폭을 전환 초반에 낮춥니다.
6. 전환 완료 후 현재 상태 ref를 갱신합니다.
7. idle 진폭을 다시 원래 크기로 올립니다.

현재 상태를 전환 시작 시점에 미리 바꾸지 않고 완료 시점에 갱신하는 것이 중요합니다. 중간에 여러 요청이 들어올 때 논리 상태와 화면 상태가 엇갈리는 것을 막습니다.

원본은 queue 모드도 설명하지만, 직접 조작하는 감정 UI에는 kill-and-continue 방식이 더 자연스럽습니다. queue는 키오스크나 자동 재생처럼 모든 포즈를 순서대로 보여야 할 때 적합합니다.

## 6. idle 모션과 전환의 합성

원본에서 가장 중요한 구현 원칙 중 하나는 idle 타임라인을 상태가 바뀔 때마다 제거하고 다시 만들지 않는 것입니다. idle 인스턴스는 컴포넌트가 마운트된 뒤 계속 살아 있습니다. 현재 상태와 진폭만 ref로 읽습니다.

전환 중에는 idle을 완전히 0으로 만들지 않습니다. 원본 가이드는 약 10%를 남기고, 전체 전환 시간의 약 40% 동안 sine in-out 곡선으로 낮췄다가 다시 올리는 값을 제안합니다. 0이 되면 캐릭터가 순간적으로 죽은 것처럼 보이고, 너무 많이 남으면 모프와 호흡이 시각적으로 경쟁합니다.

바닥에 서는 캐릭터의 회전과 scale 기준점은 발 중앙인 `50% 100%`가 자연스럽습니다. 중심점 회전은 캐릭터가 공중에 매달린 것처럼 보이게 합니다.

원본에서 제공하는 대표 idle 어휘는 다음과 같습니다.

- breathe: 천천히 1.0에서 약 1.02 사이를 왕복하는 호흡
- breathe-y: 세로 축 중심의 호흡
- sway: 발 중앙을 기준으로 작은 좌우 회전
- bob: 기준 위치보다 조금 위로 올라갔다 돌아오는 움직임
- shake: 짧은 주기의 작은 회전
- blink: 눈 파츠의 Y scale을 짧게 줄였다 복원
- look-around: 눈과 함께 입이나 얼굴 파츠를 같은 방향으로 이동
- compound: 몸, 팔, 귀 등 서로 다른 기준점을 가진 모션의 조합

## 7. Animator Studio의 역할

Animator Studio는 단순한 플레이어가 아니라 조정과 진단 도구입니다.

- 상태 버튼을 빠르게 눌러 인터럽트 안정성을 확인합니다.
- easing과 duration을 시각적으로 조절합니다.
- 파츠별 전략과 anchor를 검사합니다.
- idle 프리셋을 상태별로 바꿉니다.
- 일치하지 않는 경로 구조를 디버그합니다.
- Web, PNG sequence, SwiftUI 형식으로 내보냅니다.

원본이 브라우저 편집기를 단독 HTML로 제공하는 이유는 별도 앱 설치 없이 states 파일을 넣어 확인하게 하기 위해서입니다. React와 Babel은 CDN으로 불러오므로 첫 실행에는 인터넷 연결이 필요합니다.

## 8. 모션 디자인 원칙

원본 문서가 강조하는 애니메이션 원칙은 네 가지입니다.

### easing

감정 전환에 linear easing을 사용하면 기계적으로 보입니다. 부드러운 양방향 전환에는 power2 in-out 또는 유사한 cubic bezier, 도착감을 강조할 때는 빠르게 시작해 부드럽게 끝나는 곡선이 적합합니다.

### anticipation

큰 표정 변화 직전에 몸을 아주 조금 반대로 눌러 주면 무게가 생깁니다. 다만 작은 표정이나 졸린 전환에는 과장된 anticipation이 오히려 방해가 됩니다.

### follow-through

눈, 입, 몸, 꼬리가 정확히 같은 프레임에 출발하고 멈추면 로봇처럼 보입니다. 얼굴 감정은 눈, 눈썹, 입, 몸 순서의 짧은 겹침이 자연스럽습니다. 꼬리와 귀는 몸이 거의 도착한 뒤 조금 더 움직일 수 있습니다.

### continuous life

상태 사이에 아무 움직임이 없으면 캐릭터는 아이콘처럼 보입니다. 낮은 진폭의 호흡과 눈 깜빡임은 같은 그림을 캐릭터로 읽히게 만드는 가장 큰 차이입니다.

## 9. 예제 캐릭터의 조형 규칙

예제의 동물 캐릭터는 서로 다른 종이지만 공통 시각 문법을 공유합니다.

- 하나의 큰 둥근 몸 또는 머리 실루엣이 화면의 대부분을 차지합니다.
- 파츠 수를 줄이고 눈, 입, 귀의 작은 변화로 성격을 만듭니다.
- 검은 점형 눈과 둥근 stroke 입을 사용합니다.
- 손과 발은 해부학적 묘사보다 덩어리감이 우선입니다.
- 종 구분은 귀 실루엣, 몸의 외곽선, 작은 팔레트 차이로 만듭니다.
- stroke 굵기와 둥근 line cap을 모든 캐릭터에서 유지합니다.
- 원근과 재질 표현을 줄이고 색면과 silhouette로 읽히게 합니다.

이번 데모는 이 조형 원칙을 따르되 예제의 SVG 경로를 복사하지 않았습니다.

## 10. 이번 데모의 아키텍처 대응

원본 엔진은 상태마다 전체 path 좌표를 보관하는 범용 모프 시스템입니다. 이번 데모는 5종 x 16감정에 필요한 데이터 양을 줄이되 전환 품질을 유지하도록, 동일한 토폴로지를 가진 파츠의 좌표와 transform을 직접 수치 보간합니다.

### 공통 토폴로지

모든 캐릭터는 원본 Critters 예제와 같은 10개 파츠 토폴로지를 공유합니다.

- legL, legR
- earL, earR
- body
- eyeL, eyeR
- mouth
- handL, handR

head와 body를 분리하지 않고 하나의 큰 불규칙 실루엣으로 묶었습니다. 눈썹, 코, 볼, 눈물, 하트 같은 감정 전용 장식은 만들지 않았습니다. 종별 차이는 몸과 귀 path, 기준 좌표, 팔레트에만 있으며 감정 데이터는 항상 같은 10개 파츠에 적용됩니다.

### 감정 프로필

각 감정은 `rigX`, `rigY`, `rigRot`, `earLRot`, `earRRot`, 양쪽 눈의 위치·반경·회전, `mouthW`, `mouthCurve`, 양손과 양발 transform 같은 수치로 구성됩니다.

`requestAnimationFrame` 루프는 화면에 마지막으로 그린 수치를 ref에 보존합니다. 사용자가 빠르게 다른 감정을 누르면 기존 프레임을 취소하고 바로 그 중간값에서 새 목적 포즈로 이어집니다. 귀, 눈, 입, 손에는 짧은 지연을 주어 모든 파츠가 같은 프레임에 멈추는 기계적인 인상을 줄였습니다.

표정 전용 DOM을 추가하거나 숨기지 않으므로 상태를 바꿔도 이전 표정이 남을 수 없습니다. 모든 감정에서 SVG 요소 수는 path 4개, ellipse 4개, rect 2개로 고정됩니다.

### 입 모양

입 선은 모든 감정에서 같은 `M C` 구조를 유지합니다. 너비와 cubic control point의 Y값만 바뀌므로 미소와 찡그림 사이가 찢어지지 않고 자연스럽게 보간됩니다. 놀람도 새 입 파츠를 만들지 않고 같은 입 곡선의 폭과 깊이를 바꿉니다.

립싱크는 현재 mouth path의 Y scale을 0.9에서 1.14 범위로만 움직입니다. 새 음소 형태로 바꾸지 않기 때문에 선택한 감정의 원래 입 모양을 유지합니다.

### 모션 레이어

1. `breath-layer`: 지속 호흡
2. `activity-layer`: 듣기, bright bob, tense micro-shake
3. `morph-rig`: 현재 감정의 몸 transform
4. 개별 파츠: 귀, 눈, 입, 손, 발
5. `mouth-motion`: 선택한 입 형태를 유지하는 립싱크

부모와 자식에 transform을 나누어 적용했기 때문에 호흡이 귀 회전이나 입 scale을 덮어쓰지 않습니다.

### 듣기

듣기 모드는 몸 전체를 약간 기울이고 양쪽 귀 surface에 서로 다른 짧은 twitch를 합성합니다. 토끼의 긴 귀와 강아지의 늘어진 귀가 같은 작은 각도 변화에도 다르게 읽힙니다. 듣기 모드는 bright·tense 에너지 모션보다 우선합니다.

### 에너지 밴드

16개 감정은 calm, bright, tense, low의 에너지 밴드를 가집니다. 신남과 놀람은 작은 bob, 화남은 매우 작은 shiver, 슬픔과 졸림은 느린 호흡을 사용합니다. 모션 다양성을 위한 다양화가 아니라 감정의 에너지가 달라질 때만 idle 특성이 달라지게 했습니다.

## 11. 접근성과 성능

- 모든 선택 버튼은 `aria-pressed`를 사용합니다.
- 캐릭터 SVG는 종과 감정을 포함한 동적 대체 설명을 가집니다.
- 키보드 focus ring을 제공합니다.
- `prefers-reduced-motion`에서는 반복 애니메이션과 transition을 사실상 제거합니다.
- idle은 transform과 opacity만 사용합니다.
- React state는 버튼 선택처럼 불연속 이벤트에만 사용합니다.
- 지속 모션은 React 재렌더가 아니라 CSS compositor에서 실행됩니다.
- 모바일에서는 내부 grid의 최소 너비를 0으로 제한해 패널이 화면 밖으로 잘리지 않게 합니다.

## 12. 라이선스 판단

저장소 코드 자체는 MIT이지만 `examples/`의 아트워크는 CC BY-NC 4.0입니다. 예제 그림을 그대로 가져온 결과물은 상업 사용에 제한이 생길 수 있습니다.

이번 결과물은 다음 원칙을 적용했습니다.

- 예제의 path 데이터를 복사하지 않았습니다.
- 예제의 캐릭터를 trace하지 않았습니다.
- 공통 파츠 ID, 전략 분리, 상태 머신, idle 합성 같은 기술 원칙만 분석했습니다.
- 둥근 색면, 단순한 점형 눈, 2색 종 구분이라는 추상적 조형 문법으로 새 캐릭터를 제작했습니다.

상업 프로젝트로 확장할 때도 자체 제작한 아트워크만 유지하거나 별도로 권리를 확보한 캐릭터 소스를 사용해야 합니다.

## 13. 다음 확장 지점

- 실제 음성 amplitude를 Web Audio API로 받아 립싱크 강도를 연결
- 감정별 지속 시간과 easing을 편집하는 슬라이더
- 감정 프로필을 JSON으로 내보내고 불러오는 기능
- 눈동자의 포인터 추적을 Motion Value 또는 직접 DOM transform으로 추가
- 캐릭터별 고유 idle 추가: 강아지 꼬리 흔들기, 고양이 꼬리 끝 움직임, 토끼 귀 교차 움직임
- SVG sprite 또는 Web Component로 패키징
- 각 감정의 PNG sequence와 투명 배경 export
